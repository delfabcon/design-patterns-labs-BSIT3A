# Design Patterns Labs

This repository contains the laboratory exercises for Design Patterns.

## Lab 1

To run the Lab 1 script with the pattern applied, navigate to the `lab1_adapter` directory and execute the following command:

```bash
cd lab1_adapter
php with_pattern.php
```
